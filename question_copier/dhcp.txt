>- Zweryfikowane, przez MW
>- Sieci++ strony 1-11

MULTIPLE_CHOICE
Wskaż możliwości oferowane przez protokół DHCP:
+Dynamiczny przydział adresu logicznego z pull adresów
+Czasowa dzierżawa przydzielonego adresu logicznego
+Konfiguracja innych niż adres logiczny parametrów hosta
+Rozszerzalność zestawu wartości konfiguracyjnych
+Statyczne mapowanie adresu fizycznego na adres logiczny

MULTIPLE_CHOICE
Które zdarzenia powodują przejście klienta w stan INIT-REBOOT?
+Uruchomienie klienta gdy posiada on ważną dzierżawę
Otrzymanie odmowy dzierżawy (DHCPNAK)
Otrzymanie potwierdzenia dzierżawy (DHCPACK)
Wykrycie przez klienta, że oferowany w dzierżawie adres jest już w użyciu przez Innego hosta
Otrzymanie oferty dzierżawy (DHCPOFFER)

MULTIPLE_CHOICE
Które zdarzenia powodują przejście klienta w stan BOUND?
Wykrycie przez klienta, że oferowany w dzierżawie adres jest już w użyciu przez innego hosta
Otrzymanie odmowy dzierżawy (DHCPNAK)
Otrzymanie oferty dzierżawy (DHCPOFFER)
>- Na dysku jest jako poprawna, ale najpierw przechodzi w INIT-REBOOT, a dopiero potem jak serwer powie, że dalej jest ważna przechodzi w BOUND.
Uruchomienie klienta, gdy posiada on ważną dzierżawę
+Otrzymanie potwierdzenia dzierżawy (DHCPACK)

MULTIPLE_CHOICE
Które ze zdarzeń powodują unieważnienie dzierżawy przez serwer DHCP?
Upłynięcie połowy okresu ważności dzierżawy bez aktywności ze strony klienta
+Jawne zwolnienie dzierżawy przez klienta (komunikat DHCPRELEASE)
+Upłynięcie okresu ważności dzierżawy bez aktywności ze strony klienta

MULTIPLE_CHOICE
>- Trochę zależy, bo jest taka flaga, którą klient może ustawić żeby ACK I NAK też dostać na broadcast
Które z komunikatów DHCP mogą być przesyłane z użyciem rozgłoszeniowego w komunikacji bezpośredniej między serwerem a klientem DHCP?
DHCPNAK
+DHCPDISCOVER
DHCPACK
+DHCPREQUEST
+DHCPOFFER

MULTIPLE_CHOICE
Jaka podsieć została zarezerwowana przez IANA na rzecz sieci lokalnych z automatyczną konfiguracją (APIPA)?
169.0.0.0/8
+169.254.0.0/16
127.0.0.0/8
192.168.1.0/24

MULTIPLE_CHOICE
Wskaż cechy dzierżawy adresu w protokole DHCP.
+Każda dzierżawa łączy adres fizyczny z adresem logicznym
Każda dzierżawa pochodzi z puli dynamicznie przydzielanych adresów logicznych
Każda dzierżawa ma określony, skończony okres ważności

MULTIPLE_CHOICE
Wskaż możliwości oferowane przez protokół RARP:
Dynamiczny przydział adresu logicznego z puli adresów
+Statyczne mapowanie adresu fizycznego na adres logiczny
Czasowa dzierżawa przydzielonego adresu logicznego
Rozszerzalność zestawu wartości konfiguracyjnych
Konfiguracja innych niż adres logiczny parametrów hosta

MULTIPLE_CHOICE
Po jakim czasie klient DHCP powinien podjąć próbę odnowienia dzierżawy?
Po upływie okresu ważności dzierżawy
Po czasie równym 87,5 okresu ważności dzierżawy
+Po czasie równym połowie okresu ważności dzierżawy

MULTIPLE_CHOICE
Wskaz cechy dzierżawy adresu w protokole DHCP:
Uzyskana dzierżawa nie może być zwolniona z inicjatywy klienta
+Dzierżawa może mieć nieskończony okres ważności
+Związek między adresem fizycznym a adresem logicznym może być statyczny

MULTIPLE_CHOICE
W jaki sposób zapewniana jest unikalność adresów w automatycznej konfiguracji adresu lokalnego (APIPA)?
+Host przed przyjęciem danego adresu próbuje sprawdzić jego obecność w sieci używając zapytania ARP
Nie ma takiej konieczności, sama procedura generowania adresu gwarantuje jego unikalność
Host przed przyjęciem danego adresu próbuje sprawdzić jego obecność w sieci używając komunikatu ICMP Echo Request

MULTIPLE_CHOICE
Które zdarzenia powodują przejście klienta w stan INIT?
Otrzymanie potwierdzenia dzierżawy (DHCPACK)
+Otrzymanie odmowy dzierżawy (DHCPNAK)
+Wykrycie przez klienta, że oferowany w dzierżawie adres jest już w użyciu przez innego hosta
Uruchomienie klienta, gdy posiada on ważną dzierżawę
Otrzymanie oferty dzierżawy (DHCPOFFER)

OPEN
Jaki protokół jest bezpośrednim poprzednikiem DHCP (podaj akronim lub jego rozwinięcie do pełnej nazwy)?
BOOTP

MULTIPLE_CHOICE
Usługa mDNS:
Wymaga zapewnienia dostępności Serwera ONS
Działa w oparciu o protokół transportowy TCP
+Wykorzystuje numer portu 5353

MULTIPLE_CHOICE
Klient DHCP jest przypisany do portu:
67
80
+68
Żadnego z wymienionych
5353
Efemerycznego

MULTIPLE_CHOICE
Alokacja DHCP, w której zgłaszającemu się klientowi przydzielany jest na stałe jeden z wolnych adresów IP z określonej puli adresów, jest określana jako alokacja:
Dynamiczna
Automatyczna
Ręczna
+Statyczna

MULTIPLE_CHOICE
Po wygaśnięciu zegara T2 (rebinding timer):
Klient resetuje zegar T1 (renew timer)
Klient wysyła komunikat DHCP Request do serwera, który przyznał dzierżawę
+Klient wysyła komunikat DHCP Request, którego adresatem jest każdy aktywny serwer DHCP
Klient zwalnia dzierżawę i przechodzi do stanu INIT


MULTIPLE_CHOICE
>- Offer to chyba jak jest rebinding do innego serwera zanim poprzednia dzierżawa się zakończy
Które z komunikatów DHCP mogą być przesyłane z użyciem adresowania jednoznacznego (unicast) w komunikacji bezpośredniej między serwerem a klientem DHCP?
+DHCPNAK
+DHCPACK
DHCPDISCOVER
+DHCPREQUEST
+DHCPOFFER

MULTIPLE_CHOICE
W odpowiedzi na wysłany przez klienta komunikat DHCPDiscover serwer DHCP wysyła komunikat:
DHCP Rquest
DHCP Decline
DHCP ACK
+DHCP Offer
DHCP Inform

MULTIPLE_CHOICE
>- nw czemu nie automatyczna
Alokacja DHCP w które następuje przydział adresu IP naokreślony, skończony czas zwany czasem dzierżawy jestokreślana jako alokacja:
+Dynamiczna
Automatyczna
Ręczna
Statyczna

MULTIPLE_CHOICE
Przestrzeń adresowa zarezerwowana przez IANA na potrzebykonfiguracji węzłów w sieci IPv4 to:
+192.168.0.0 255.255.0.0
169.254.0.0 255.255.0.0
172.16.0.0 255.240.0 .0
10.0.0.0 255.0.0.0

MULTIPLE_CHOICE
Jaki rodzaj alokacji DHCP zapewnia najbardziej efektywną obsługę sieci o dużej dynamice zmian?
Alokacja ręczna
Alokacja statyczna
Alokacja automatyczna
+Alokacja dynamiczna

SINGLE_CHOICE
W przypadku zapytania klienta przekazanego przez zdalny przekaźnik DHCP Relay, serwer DHCP indentyfikuje adres, na który należy wysłać odpowiedź na podstawie adresu zapisanego w polu:
Server IP Address (SIADDR)
+Gateway IP Address (GIADDR)
Client Hardware Address (CHADDR)
Your IP Address (YIADDR)
Client IP Address (CIADDR)

MULTIPLE_CHOICE
W jaki sposób globalna pula adresów IP jest dystrybuowanapomiędzy użytkowników sieci?
Dystrybucja puli adresów globalnych jest wynikiem dobrowolnych porozumieńzawieranych przez dostawców (ISP)
Nie ma zasad w tej mierze, administrator hosta może dobrać adreswedług własnej woli
+Adresy są centralnie rozdzielane przez IANA pomiędzy rejestry regionalne, ich operatorzyw ramach otrzymanych przydziałów dystrybuują adresy pomiędzy dostawców (ISP)
Użytkownicy końcowi uzyskują przydział adresów bezpośrednio od IANA

MULTIPLE_CHOICE
Wskaż pule adresów prywatnych zgodnie z RFC 1918:
+172.16.0.0 - 172.31.0.0/12
+192.168.0.0 - 192.168.255.0/24
169.254.0.0/16
127.0.0.0/8

